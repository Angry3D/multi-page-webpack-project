# 归档：AI 工作流文档标准迁移

日期：2026-05-29

## 目标

将项目 AI 工作流文档迁移到 `ai-project-workflow` 标准结构。

## 关联待办

- TODO-0001

## 计划

1. 使用 `AGENTS.md` 作为 AI 入口指南。
2. 使用 `docs/project-facts.md` 记录稳定项目事实。
3. 使用 `docs/backlog.md` 作为待办资源池。
4. 使用 `docs/progress.md` 记录当前执行阶段。
5. 使用 `docs/archive/` 沉淀已完成阶段。

## 完成内容

- 已建立 `AGENTS.md`、`docs/project-facts.md`、`docs/backlog.md`、`docs/progress.md` 和 `docs/archive/` 的标准文档结构。
- 已用 `docs/backlog.md` 替代原先非标准的 backlog 命名约定。
- 已明确文档工作流：讨论 -> backlog -> progress -> 执行 -> archive -> 下一轮。
- 已将 TODO-0001 从 `done` 更新为 `archived`。

## 验证

- 已确认标准文档文件存在。
- 已确认 `docs/backlog.md`、`docs/progress.md` 和 `docs/archive/` 均按当前工作流使用。
- 已确认 TODO-0001 状态更新为 `archived`。

## 决策

- 采用 `ai-project-workflow` 标准结构作为后续 AI 协作入口。
- 保持文件路径为英文，正文语系可按项目需要维护。

## 后续事项

- 当前 P0 已闭环，后续可继续推进 P1 构建优化项。
